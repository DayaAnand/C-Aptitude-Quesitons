#include<stdio.h>
int main()
{
if(printf("Coding")) //returns the length of the string, here it is 6 and prints Coding to the output as 6(non-zero value) is true for the if condition
    switch(printf("is")) //returns the length of the string, here it is 2 and prints is and it checks for case 2 and as case 2 is not found the program terminates here
        while(printf("fun")) 
return 0;
}