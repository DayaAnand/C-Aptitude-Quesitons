#include<stdio.h>
int main()
{
if(printf("Coding")) //returns the length of the string, here it is 6 and prints Coding to the output as 6(non-zero value) is true for the if condition
    switch(printf("\n")) //returns the length of the string, here it is 1 and it checks for case 1 and case 1 is found and prints the print statement in the next line
        case 1:
        printf("is fun");
return 0;
}