#include <stdio.h>

int main(){
    int x,y; 
    printf("%d",scanf("%d%d",&x,&y)); //this returns 2 as the output because the scanf statement return input value to the print function
    //this is int main() return
    return 1; //scanf return no of successful inputs if one input value given it returns 1
}
